server:
  port: 8081
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: 124.220.50.39:8848
        namespace: cloud-demo
        file-extension: yaml
        shared-configs:
          - data-id: shared-config
            refresh: true
